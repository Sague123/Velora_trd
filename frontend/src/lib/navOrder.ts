/** Left-to-right order of the authenticated shell's top-level tabs. Shared
 * by AppLayout (to pick a slide direction that matches how a nav click would
 * move) and TopBar (to pick the next/previous tab on a swipe gesture). */
export const NAV_ORDER = [
  "/overview",
  "/terminal",
  "/markets",
  "/strategies",
  "/savings",
  "/crm",
  "/admin",
  "/profile",
];
