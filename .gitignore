node_modules/
dist/
.env
.env.local
*.log

# Legacy SQLite artifacts — the app is Postgres-only now (see DEPLOY.md);
# these are just stray local files from before the migration.
*.db
*.db-journal
*.db-shm
*.db-wal
local-dev.log
server-test*.log
